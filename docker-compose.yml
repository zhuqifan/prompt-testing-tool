services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      # 挂载数据目录，确保数据持久化
      - ./data:/app/data
    environment:
      - PORT=3001
      - DB_PATH=/app/data/data.db
      - NODE_ENV=production
    restart: unless-stopped

